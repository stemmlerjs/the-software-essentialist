import { BrowserDriver } from "../browserDriver";

export class FrontPage {
  private driver: BrowserDriver;

  constructor (driver: BrowserDriver) {
    this.driver = driver;
  }

  expectToHaveNavigatedHere () {
    
  }
  
  
}
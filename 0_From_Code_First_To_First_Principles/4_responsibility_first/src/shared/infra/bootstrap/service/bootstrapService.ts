


// npm run start --as-roles app relay worker --env development

export class Bootstrap {
  generateApplicationConfigFromCLI (startCommand: string) {

  }
}

// const { spawn } = require('child_process');

// // Spawn first Node.js process
// const process1 = spawn('node', ['script1.js']);

// // Spawn second Node.js process
// const process2 = spawn('node', ['script2.js']);

// // Event handlers for process output
// process1.stdout.on('data', (data) => {
//   console.log(`Process 1 output: ${data}`);
// });

// process2.stdout.on('data', (data) => {
//   console.log(`Process 2 output: ${data}`);
// });

// // Event handler for process exit
// process1.on('exit', (code) => {
//   console.log(`Process 1 exited with code ${code}`);
// });

// process2.on('exit', (code) => {
//   console.log(`Process 2 exited with code ${code}`);
// });
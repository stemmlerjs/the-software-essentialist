
describe('bootstrapService', () => {

  /**
   * Given the application isn't started
   * When I try to start the application in dev mode
   * And I want to run all of the system's roles (3 of them) on the same machine
   * Then I should see that the application is started
   * And there are 3 child processes running 
   * And the application is running in development mode
   */

  test(`3 processes in a single application in dev mode`, () => {
    // Given 

    // Then
    // expect(processManager.)
  })
})
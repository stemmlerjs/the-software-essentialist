
describe("postsController", () => {
  it('can get posts', () => {

    // Arrange
      // Started up the server w/ the controller
      // Created a mock for the application object's postsService's getPosts to return a pre-defined set of data
      // Replaced the post

    // Act
      // Use the client library to make the api call to get the poss

    // Assert
      // Communication: Expect it to have called the correct use case
      // Result: Expect the shape of the data that came back to be correct

  })
})